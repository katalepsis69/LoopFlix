import TerminalClient from "@/components/pages/TerminalClient";

export const metadata = {
  title: "Terminal — LoopFlix",
  description: "LoopFlix archive terminal. Search, play, and browse with commands.",
};

export default function TerminalPage() {
  return <TerminalClient />;
}
